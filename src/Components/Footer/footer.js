import styles from "./footer.module.css"

const footer = () => {
    return <footer className={styles.footer}>
        <div className={styles.footer_inner}>
            <h1>Nikitoshas devs studio</h1>
        </div>
    </footer>
}

export default footer;